{
  "name": "garm-admin-backend",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "description": "Real backend for the Garm Admin Portal — REST API + live push (Server-Sent Events) to the Garm customer app. Zero external dependencies: pure Node.js, no npm install required.",
  "scripts": {
    "dev": "node --watch index.js",
    "start": "node index.js",
    "seed:reset": "node reset-db.js",
    "simulate": "node simulate-order.js"
  },
  "dependencies": {},
  "engines": { "node": ">=18" }
}
