# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Backend secrets/data — never commit. The encryption key protects every
# customer's PII at rest; db.json is the live database (encrypted fields,
# but still the real data). See server/README.md.
server/.server-secret
server/db.json
.env
.env.local
